apiVersion: v1
kind: Service
metadata:
  name: {{ include "sigma-rs.fullname" . }}
  labels:
    {{- include "sigma-rs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "sigma-rs.selectorLabels" . | nindent 4 }}